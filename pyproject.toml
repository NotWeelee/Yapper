[project]
name = "yapper"
version = "0.2.0"
description = "Security testing tool for voice-based AI agents"
requires-python = ">=3.10"
dependencies = [
    "flask",
    "twilio",
    "pyyaml",
    "httpx",
    "anthropic",
    "openai>=1.0",
    "python-dotenv",
]